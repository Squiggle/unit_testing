/**
 * Resolve a lat / lon from a given address
 * @param address as string
 * @returns { lat: number, lon: number }
 */
export const resolveAddress = (address: string) => ({
  lat: 51.46, lon: -0.128
});